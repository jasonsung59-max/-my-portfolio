<!DOCTYPE html>
<html lang="en">
<head> <link rel="manifest" href="manifest.json">
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Creator Portfolio</title>
  <style>
    :root {
      --bg: #121214;
      --card-bg: #1e1e22;
      --accent: #8a2be2;
      --text: #e1e1e6;
      --text-muted: #a0a0b0;
    }

    * { box-sizing: border-box; margin: 0; padding: 0; }
    body { font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif; background: var(--bg); color: var(--text); line-height: 1.6; padding: 2rem; }
    header { text-align: center; margin-bottom: 3rem; }
    header h1 { font-size: 2.5rem; color: #fff; margin-bottom: 0.5rem; }
    header p { color: var(--text-muted); }

    .nav-tabs { display: flex; justify-content: center; gap: 1rem; margin-bottom: 2rem; flex-wrap: wrap; }
    .tab-btn { background: var(--card-bg); color: var(--text); border: 1px solid #333; padding: 0.6rem 1.2rem; border-radius: 20px; cursor: pointer; transition: all 0.2s; }
    .tab-btn.active, .tab-btn:hover { background: var(--accent); color: #fff; border-color: var(--accent); }

    .tab-content { display: none; max-width: 1000px; margin: 0 auto; }
    .tab-content.active { display: block; }

    .grid { display: grid; grid-template-columns: repeat(auto-fill, minmax(280px, 1fr)); gap: 1.5rem; }
    .card { background: var(--card-bg); border-radius: 8px; overflow: hidden; border: 1px solid #2a2a30; }
    .card img { width: 100%; height: 200px; object-fit: cover; }
    .card-body { padding: 1.2rem; }
    .card-body h3 { margin-bottom: 0.5rem; font-size: 1.2rem; }
    .card-body p { color: var(--text-muted); font-size: 0.95rem; }

    .draft-container { background: var(--card-bg); padding: 2rem; border-radius: 8px; font-family: "Courier New", Courier, monospace; border-left: 4px solid var(--accent); }
    .tag { display: inline-block; background: #333; color: var(--accent); font-size: 0.75rem; padding: 0.2rem 0.5rem; border-radius: 4px; margin-bottom: 0.5rem; }
  </style>
</head>
<body>

  <header>
    <h1>My Creative Workspace</h1>
    <p>Novels, illustrations, early drafts, and creation notes.</p>
  </header>

  <div class="nav-tabs">
    <button class="tab-btn active" onclick="openTab(event, 'novels')">Novels</button>
    <button class="tab-btn" onclick="openTab(event, 'artwork')">Artwork</button>
    <button class="tab-btn" onclick="openTab(event, 'drafts')">Raw Drafts</button>
    <button class="tab-btn" onclick="openTab(event, 'bts')">Behind the Scenes</button>
  </div>

  <section id="novels" class="tab-content active">
    <div class="grid">
      <div class="card">
        <img src="https://via.placeholder.com/300x200" alt="Novel Cover">
        <div class="card-body">
          <span class="tag">Published Work</span>
          <h3>Title of Your Novel</h3>
          <p>A brief synopsis or logline of your novel to hook readers into your story world.</p>
        </div>
      </div>
    </div>
  </section>

  <section id="artwork" class="tab-content">
    <div class="grid">
      <div class="card">
        <img src="https://via.placeholder.com/300x200" alt="Illustration">
        <div class="card-body">
          <span class="tag">Digital Art</span>
          <h3>Character Concept</h3>
          <p>Description of the piece, medium used, or character lore.</p>
        </div>
      </div>
    </div>
  </section>

  <section id="drafts" class="tab-content">
    <div class="draft-container">
      <span class="tag">Unedited Chapter Excerpt</span>
      <h3>Chapter 1 (Work in Progress)</h3>
      <br>
      <p>The wind bit sharply through the trees as dark clouds gathered above...</p>
    </div>
  </section>

  <section id="bts" class="tab-content">
    <div class="grid">
      <div class="card">
        <img src="https://via.placeholder.com/300x200" alt="Process">
        <div class="card-body">
          <span class="tag">Worldbuilding</span>
          <h3>Map Design Process</h3>
          <p>Insights into how the world map was drafted from sketch to final render.</p>
        </div>
      </div>
    </div>
  </section>

  <script>
    function openTab(evt, tabName) {
      const contents = document.querySelectorAll('.tab-content');
      contents.forEach(content => content.classList.remove('active'));

      const buttons = document.querySelectorAll('.tab-btn');
      buttons.forEach(btn => btn.classList.remove('active'));

      document.getElementById(tabName).classList.add('active');
      evt.currentTarget.classList.add('active');
    }
  </script>

</body>
</html> 
